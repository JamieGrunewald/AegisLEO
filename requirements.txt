# ──────────────────────────────────────────────
# AegisLEO Runtime Dependencies
# ──────────────────────────────────────────────

# Post-Quantum Cryptography (liboqs Python bindings)
# Install from source: https://github.com/open-quantum-safe/liboqs-python
# oqs is not on PyPI — see docs/pqc_design.md for setup instructions

# Symmetric crypto & KDF (AES-256-GCM, HKDF-SHA256)
cryptography>=42.0.0

# LoRa serial link
pyserial>=3.5

# MQTT transport (optional — used by mqtt_sender / mqtt_receiver)
paho-mqtt>=2.0.0

# Structured logging
loguru>=0.7.0

# Rich terminal output (demo logging)
rich>=13.0.0

# ──────────────────────────────────────────────
# ML Training Dependencies (models/ scripts)
# ──────────────────────────────────────────────
# PyTorch — install separately per your platform:
#   https://pytorch.org/get-started/locally/
# torch>=2.0.0

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ──────────────────────────────────────────────
# Development / Testing
# ──────────────────────────────────────────────
pytest>=7.0.0
